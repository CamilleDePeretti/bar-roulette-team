
<% if night.bars.length == 6 %>
  <% wheel_text = "Show more" if night.page == 1 %>
  <% wheel_text = "Show previous bars" if night.page == 2 %>

  <%= link_to wheel_text, night_update_path(night), method: :patch, class: 'btn btn-primary' %>
<% end %>

