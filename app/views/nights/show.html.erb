<h4>Let the magic begin...</h4>

<div>
  <%= @night.lat %>
  <%= @night.lng %>
</div>

<div
  id="map"
  style="width: 100%;
  height: 300px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>

  <a id="url" href="#">
    Copy to clipboard
  </a>

<script>

  btn = document.querySelector("#url");
  btn.addEventListener("click", (event) => {
    updateClipboard();
  });
  function updateClipboard() {
  navigator.clipboard.writeText("<%= request.original_url %>").then(function() {
    console.log("successful")
    btn.insertAdjacentHTML("afterend", "<%= flash[:alert] = 'Link copied to clipboard' %>");
     alert("Link copied to clipboard");
  }, function() {
    console.log("failed");
  });
}
</script>



<% @results.each do |result| %>
    <%= result[1] %>
<% end %>

